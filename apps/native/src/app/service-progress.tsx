/** 루트 상세 라우트 `/service-progress` → 전체 화면 복무율. */

import { ServiceProgressDetailScreen } from "@/screens/service-progress-detail";

export default function ServiceProgressRoute() {
  return <ServiceProgressDetailScreen />;
}
