-- 정기외박이 주기마다 소멸하지 않고 쌓이는 부대가 있다. 공개 규정이 정하는 것은
-- "지휘관이 승인한다"와 외박 시간 상한뿐이고 이월 여부는 부대 지침이라
-- (regular-overnight-guidance.ts) 앱이 한쪽을 고를 수 없다. 그래서 사용자가 켠다.
--
-- 기본값 0인 이유: 지금까지의 모든 행은 주기별 소멸로 계산돼 왔다. 마이그레이션 한 번에
-- 그 사람들의 잔여가 늘어나면 화면의 숫자가 이유 없이 바뀐다. 켜는 것은 사용자의 선택이다.
--
-- 켠 시점을 따로 적지 않는다. 적는 순간 같은 설정이 "언제 켰는지"에 따라 다른 잔여를
-- 내는 상태가 생기고, 그 시점은 되돌릴 수도 고칠 수도 없다. 지금은 첫 적립일부터 전부
-- 소급되고, 끄면 곧바로 주기별 셈으로 돌아온다.
ALTER TABLE `regular_overnight_configs` ADD COLUMN `carry_over` integer DEFAULT 0 NOT NULL;
