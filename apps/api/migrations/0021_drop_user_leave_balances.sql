-- 쓰지 않는 user_leave_balances 표를 지운다.
--
-- 휴가 총량은 0010에서 적립분 원장(leave_grants)으로 옮겨 갔고, 그 뒤로 이 표를
-- 읽는 코드도 쓰는 코드도 없다. 남아 있는 동안에는 "둘 중 어느 쪽이 진짜인가"를
-- 매번 다시 확인해야 하는 비용만 있었다.
--
-- 지우기 전에 운영 데이터를 확인했다: 남은 10행은 모두 한 사용자의 것이고,
-- 값이 있는 항목(연가 28, 포상 18, 위로 3)은 전부 같은 재원의 leave_grants 행으로
-- 이미 옮겨져 있다. 나머지는 0이라 옮길 것이 없었다.
-- regular_overnight의 음수 조정값(-12)은 의도적으로 옮기지 않은 값이다 —
-- 정기외박은 0009에서 원장 모델을 되돌려 주기 설정에서 파생하도록 바뀌었다.
DROP TABLE `user_leave_balances`;
