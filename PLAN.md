# 사용자 접속 기록

- 각 플랫폼 별로 사용자 동의 없이 수집 가능한 모든 정보를 수집
- 수집한 정보를 데이터베이스에 저장
- App Push Notification 수신 및 송신 데이터 모두 저장
- 무조건 각 플랫폼 별로 사용자 동의 없이 수집 가능한 정보를 모두 수집하고 해당 정보를 모두 D1 데이터 베이스에 저장해야 함

# Turborepo 최적화

- 코드 재사용성을 높이기 위해 중복된 코드는 packages로 분리하여 각 app에 의존성으로 등록 후 사용함
- 엔터프라이즈급 코드 퀄리티를 위해 코드 재사용성을 높일수 있도록 코드 구조 개편
- 엔터프라이즈급 코드 퀄리티를 위해 유지보수성을 높일 수 있도록 코드 구조를 구성
- 모든 코드에 대해 이해하기 쉬운 한국어 주석을 작성함
- 모든 apps 안에 있는 프로젝트를 조작할 수 있는 명령어를 turbo.json에 등록
- Turborepo Cache 기능을 적극적으로 사용할 수 있도록 설정

# 백엔드 성능 향상

- Cloudflare Workers 기반 런타임에 맞춰 코드를 최적화하여 백엔드 성능 극대화
- Cloudflare KV를 사용하여 성능 최적화 진행

# 테스트 작성

- Expo App에서 모든 기능에 대한 e2e 테스트 작성 및 검증
- API 개발할 때 TDD 기반으로 개발 진행
- API에서 모든 기능에 대한 unit test 작성 및 검증
- Web에서 모든 기능에 대한 e2e 테스트 작성 및 검증
